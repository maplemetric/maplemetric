@ApplicationModule(
        allowedDependencies = {
                "common",
                "common::nexon"
        }
)
package com.maplemetric.event;

import org.springframework.modulith.ApplicationModule;
