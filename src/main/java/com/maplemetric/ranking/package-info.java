@ApplicationModule(
        allowedDependencies = {
                "common",
                "common::nexon"
        }
)
package com.maplemetric.ranking;

import org.springframework.modulith.ApplicationModule;
